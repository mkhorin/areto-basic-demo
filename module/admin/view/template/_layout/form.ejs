<% layout(_view.get('_layout/content')) %>

<% if (model.hasError()) { %>
<div class="row">
    <div class="col-sm-10 col-sm-offset-2">
        <div class="alert alert-danger">
            <% for (const attr of Object.keys(model.getErrors())) { %>
            <p>
                <b><%- _t(model.getAttrLabel(attr)) %>:</b>
                <%- _t(model.getFirstError(attr))%>
            </p>
            <% } %>
        </div>
    </div>
</div>
<% } else if (_controller.isPost()) { %>
<div class="row">
    <div class="col-sm-10 col-sm-offset-2">
        <div class="alert alert-success">The object is stored</div>
    </div>
</div>
<% } %>

<form action="<%= _controller.getOriginalUrl() %>" method="post" class="form-horizontal form" autocomplete="off">

    <input type="hidden" name="referrer" value="<%= _controller.getRefUrl() %>">

    <div class="object-controls">
        <button class="btn btn-primary" type="submit"><%- _t('Save') %></button>

        <% if (!model.isNew()) { %>

        <a href="<%- _url(['view', model]) %>" class="btn btn-success">View</a>

        <button class="btn btn-danger action-submit" type="button"
                data-url="<%- _url(['delete', model]) %>"
                data-confirm="Remove this object?">
            Delete
        </button>
        <% } %>
    </div>

    <%- body %>

</form>