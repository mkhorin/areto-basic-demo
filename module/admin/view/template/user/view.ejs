<% layout(_view.get('_layout/view')) -%>

<% block('title', 'User') -%>

<% _data.set('breadcrumbs', [{
    title: 'Users',
    url: 'admin/user'
},{
    title: model.getTitle()
}]) %>

<% const stat = _view.get('_part/field/static'); %>

<%- include(stat, {
    attr: 'status'
}) %>

<%- include(stat, {
    attr: 'name'
}) %>

<%- include(stat, {
    attr: 'email'
}) %>

<%- include(stat, {
    attr: 'role'
}) %>

<%- include(stat, {
    attr: 'updatedAt',
    value: _format(model.get('updatedAt'), 'timestamp'),
    escaping: false
}) %>

<%- include(stat, {
    attr: 'createdAt',
    value: _format(model.get('createdAt'), 'timestamp'),
    escaping: false
}) %>