<% let title = 'Вход в систему'; %>

<% layout(_view.get('_layout/main')) -%>

<% block('title', title) -%>

<% _data.set('breadcrumbs', [{'label': title}]) %>

<div class="row">
    <div class="col-sm-6 col-sm-offset-3 col-lg-4 col-lg-offset-4">

        <h3 class="login-box-msg">
            <%- title %>
        </h3>

        <form action="<%- _url('sign-in') %>" method="post">
            <%- include(_view.get('_part/field/auth/input'), {
                attr: 'email',
                type: 'email',
                iconClass: 'glyphicon glyphicon-envelope',
                placeholder: 'Электронный адрес'
            }) %>

            <%- include(_view.get('_part/field/auth/input'), {
                attr: 'password',
                type: 'password',
                value: '',
                iconClass: 'glyphicon glyphicon-lock',
                placeholder: 'Пароль'
            }) %>

            <% if (model.isCaptchaRequired()) { %>
            <%- include(_view.get('_part/field/auth/captcha'), {
                attr: 'captchaCode',
                iconClass: 'glyphicon glyphicon-check',
                url: _url('captcha'),
                placeholder: 'Проверочный код'
            }) %>
            <% } %>

            <div class="row">
                <div class="col-xs-8">
                    <% if (_controller.user.config.enableAutoLogin) { %>
                    <%- include(_view.get('_part/field/checkbox'), {
                        attr: 'rememberMe',
                        label: 'Запомнить меня'
                    }) %>
                    <% } %>
                </div>
                <div class="col-xs-4">
                    <button type="submit" class="btn btn-primary btn-block btn-flat">
                        Войти
                    </button>
                </div>
            </div>
        </form>
        <br>
        <p class="text-center">
            <a href="auth/sign-up">
                Регистрация нового пользователя
            </a>
        </p>
    </div>
</div>